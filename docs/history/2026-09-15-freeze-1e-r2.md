# Freeze — ANKLA-AGT-1E-R2 (Capability Integrity & Trust Binding)

- **Fecha:** 2026-09-15
- **Tipo:** freeze record de preservación (objeto reproducible para revisión
  externa). No es una revisión ni una adjudicación externa.

```text
FREEZE_ID = ANKLA-AGT-1E-R2
FREEZE_COMMIT = f27654ad8bea3a9dbc4ff155402f2fb56979f58f
BASELINE_PARENT = fe739ade473b9fd66cf9be79567100da27eb0876

GLOBAL_VERDICT = PASS

HIGH1_STATUS = RESOLVED_BY_PRODUCER
EXTERNAL_REVIEW_STATUS = PENDING

MODEL_CALLS = 0
ANKLA_REVISION_CHANGED = NO
```

Nota de estado: `HIGH1_STATUS = RESOLVED_BY_PRODUCER` es deliberado. NO se
declara `VERIFIED_RESOLVED` hasta que el reviewer externo adjudique el
resultado. El commit congelado es el objeto de revisión.

## Contenido congelado

- `playground/16_capability_integrity/` — harness completo:
  `capability.py` (GrantedCapability frozen + sello HMAC de proceso),
  `capability_factory.py` (Zone/ZoneRegistry + binding policy→zone),
  `host.py`, `mechanical_suite.py`, `policy/filesystem.rego`,
  `manifest.yaml`, `fixtures/`, `evidence/` (cuatro corridas PASS
  equivalentes + audit log), `README.md` (incluye provenance: instrucciones
  truncadas tras R2-2).
- `docs/history/2026-09-15-fase-1e-r2-capability-integrity.md` — bitácora de
  la fase.

## Preflight de la congelación

```text
ENVIRONMENT_GATE = PASS
ANKLA_VERIFY = PASS
ANKLA_REVISION = sha256:59be6c561d010ecc7f1f6a4318fc05d1df05d6ef5c6975ad88be1cdec389bdfd
HEAD_BEFORE_FREEZE = fe739ade473b9fd66cf9be79567100da27eb0876
SECRET_SCAN = PASS
HISTORICAL_PHASES_MODIFIED = NO
DEPENDENCIES_CHANGED = NO
ANKLA_STATE_TRACKED = NO
ENV_TRACKED = NO
VENV_TRACKED = NO
R2_RUN_1 = PASS
R2_RUN_2 = PASS
RESULTS_EQUIVALENT = YES
FORBIDDEN_PHYSICAL_WRITES = 0
```

## TRUST_ROOTS

- ACS/OPA decision (veredicto allow con labels de identidad de zona)
- host ZoneRegistry (mapping pre-registrado `zone_id → host/guest roots`)
- GrantedCapability integrity validation (sello HMAC de proceso +
  re-verificación antes de materializar Monty)
- Monty execution boundary (mounts derivados sólo del grant verificado)

## KNOWN_LIMITATIONS

- inner operations not re-evaluated by ACS
  (`INNER_OPERATION_REEVALUATED_BY_ACS = NO`)
- TOCTOU window exists (`TOCTOU_WINDOW_EXISTS = YES`), contenida, no
  eliminada
- capability integrity review pending (confinement != universal sandbox
  security; modelo de amenaza: consumidor hostil con el objeto capability)
- ZoneRegistry external review pending (es TRUST_ROOT_COMPONENT; no se
  afirma seguridad frente a todos los ataques)

## Estado de findings

```text
A2_MOUNT_INJECTION     = CLOSED (por el productor)
A3_POST_GRANT_MUTATION = CLOSED (por el productor)
A9_RESULT_LABEL_SCOPE_TRUST = CLOSED (por el productor)
A4_E2_REPLAY           = SUSTAINED
A11/A12_NO_VERDICT_NO_CAPABILITY = PASS (sustain)
A13_REENTRANCY         = LIMITATION_PERSISTS_EFFECT_CONFINED
```

## Sin tag, sin release

No se creó tag ni release: `EXTERNAL_REVIEW_STATUS = PENDING`. El tag de
baseline experimental se decidirá después del reviewer externo.

## Detención

Después de este freeze no se ejecutan nuevas pruebas, reviewer, paper,
cambios EKTEL, upstream, tags ni releases. La siguiente acción es entregar
`FREEZE_COMMIT = f27654ad8bea3a9dbc4ff155402f2fb56979f58f` a un reviewer
externo fresco.
